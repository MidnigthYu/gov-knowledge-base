# gov-knowledge-base 政务知识库 RAG 系统
面向政务领域的智能问答系统，基于 RAG 架构实现文档解析、向量检索与大模型问答，从零搭建工业级 Python 后端工程。

## 技术栈
- Python 3.10+、requests、python-dotenv、chardet
- 大模型：智谱 AI GLM、DeepSeek
- 测试：unittest / pytest
- 工程化：虚拟环境、统一日志、环境变量管理

## 目录结构
```
├── config/    # 全局配置与环境变量
├── common/    # 公共组件（日志、自定义异常、通用工具）
├── client/    # 大模型客户端（抽象基类 + 多厂商实现）
├── utils/     # 文本清洗、文件处理工具
├── tests/     # 单元测试
├── main.py    # 项目入口
└── requirements.txt
```

## 核心实现
1. **多模型客户端架构**：基于ABC抽象基类封装智谱、DeepSeek两款国产大模型，统一调用接口，原生支持多轮对话、历史记忆管理、系统提示词、分层异常处理，扩展新模型仅需实现两个方法。
2. **政务文本清洗**：多编码自动识别+多级兜底，支持空行、冗余空格、特殊字符过滤，支持递归批量处理且单文件异常不中断整体任务。
3. **工程化基础**：密钥与代码分离、统一日志体系、自定义业务异常、配置启动校验、分层目录架构，符合工业级项目开发规范。
4. **单元测试体系**：覆盖文本清洗、文件读取、批量处理三大模块，包含正常、边界、异常三类场景，支持回归验证。

## 技术亮点
- 基于抽象基类的分层模块化设计，低耦合易扩展，符合面向接口编程规范
- 多模型可插拔架构，兼顾初期开发效率与长期架构灵活性
- 自定义分层异常体系，错误不静默，排查定位清晰
- 全场景异常兜底，保障批量任务鲁棒性
- 核心函数配套单元测试，覆盖边界与异常场景，支持重构回归验证
- 配置启动前置校验，遵循失败前置原则，问题早发现早处理

## 项目进度
✅ 已完成：工程骨架搭建、双大模型客户端抽象重构、多轮对话支持、政务文本清洗工具、全量单元测试、自定义异常与配置校验体系


## 快速开始
1. 执行单元测试：`python -m unittest discover tests -v`
2. 复制 `.env.example` 为 `.env`，填入对应 API 密钥
3. 运行项目：`python main.py`
4. 执行单元测试：`python -m unittest tests.test_text_cleaner`