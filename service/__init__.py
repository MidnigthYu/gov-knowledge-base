### README 补充：私有化部署（Docker）章节
直接将以下内容追加到现有 README 的「快速开始」章节之后，即可完成部署文档验收项，风格与原有文档完全统一：

---

## 私有化部署（Docker）
支持一键容器化编排部署，代码与数据完全分离，适配政务内网离线私有化场景，镜像可独立导出交付，运行时无公网依赖。

### 前置依赖
- Docker 20.10+ / Docker Compose v2+
- 最低配置：2核CPU / 2GB内存 / 1GB可用磁盘
- 支持纯离线内网部署，所有依赖已固化在镜像内

### 部署模式
#### 模式一：开发调试模式（源码挂载）
代码修改即时生效，适合本地开发迭代，业务数据持久化不丢失：
1.  配置环境变量：复制 `.env.example` 为 `.env`，填写大模型密钥、向量库配置等参数
2.  一键启动服务：
    ```bash
    docker-compose up -d
    ```
3.  访问地址：
    - 接口文档：http://127.0.0.1:8000/docs
    - 健康检查：http://127.0.0.1:8000/health
    - Streamlit 可视化前端：http://127.0.0.1:8501

#### 模式二：纯镜像交付模式（生产/离线）
镜像自身功能完整自洽，不依赖本地源码，可独立部署运行，适合正式交付与离线政务场景：
1.  导入交付镜像：
    ```bash
    docker load -i gov-knowledge-base-v2.0.tar
    ```
2.  配置 `.env` 环境变量与数据挂载目录
3.  修改 `docker-compose.yml` 镜像标签为 `gov-knowledge-base:v2.0`，注释所有源码挂载项
4.  启动服务：
    ```bash
    docker-compose up -d
    ```

### 持久化目录说明
所有业务数据全部外置挂载，镜像升级、容器重建不丢失业务数据：
```
data/
├── docs/          # 原始政务文档库
└── chroma/        # Chroma 向量数据库
logs/              # 服务运行日志
temp_uploads/      # 文件上传临时缓存
```

### 离线镜像导出
```bash
docker save -o gov-knowledge-base-v2.0.tar gov-knowledge-base:v2.0
```
导出的 tar 包可直接在离线政务内网环境导入部署，全程无需公网拉取任何依赖。

### 常见问题
1.  **端口占用**：修改 `docker-compose.yml` 中 `ports` 映射端口
2.  **文件上传失败**：检查宿主机 `temp_uploads` 目录读写权限
3.  **向量库连接异常**：确认 `.env` 中向量库路径与挂载目录一致
4.  **服务平滑重启**：执行 `docker-compose restart`，不丢失业务数据
5.  **静态页面访问**：原生 HTML 对话页需挂载静态目录后访问，默认推荐使用 Streamlit 前端

---

### 项目进度同步更新
在现有「项目进度」条目末尾追加：
、Docker 容器化私有化部署方案、docker-compose 一键编排、业务数据持久化、离线镜像交付支持
